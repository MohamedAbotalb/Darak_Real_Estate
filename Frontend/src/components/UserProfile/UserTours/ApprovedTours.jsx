import React from 'react';
import { Typography } from '@mui/material';

function ApprovedTours() {
  return (
    <div>
      <Typography variant="h6">Approved Tours</Typography>
      <Typography>
        This is where the content for approved tours will be displayed.
      </Typography>
    </div>
  );
}

export default ApprovedTours;
