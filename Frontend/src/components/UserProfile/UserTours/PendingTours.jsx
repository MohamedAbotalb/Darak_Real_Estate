import React from 'react';
import { Typography } from '@mui/material';

function PendingTours() {
  return (
    <div>
      <Typography variant="h6">Pending Tours</Typography>
      <Typography>
        This is where the content for pending tours will be displayed.
      </Typography>
    </div>
  );
}

export default PendingTours;
